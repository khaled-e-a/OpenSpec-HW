[package]
name = "rust-cli-calendar"
version = "0.1.0"
edition = "2021"

[dependencies]
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
proptest = "1"
portable-pty = "0.8"
