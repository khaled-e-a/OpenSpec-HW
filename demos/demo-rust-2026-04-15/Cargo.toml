[package]
name = "dashboard-grid"
version = "0.1.0"
edition = "2024"

[dependencies]
egui = "0.31"
eframe = "0.31"  # For the app framework
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
